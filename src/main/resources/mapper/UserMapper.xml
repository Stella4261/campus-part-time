<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.parttime.mapper.UserMapper">
    <select id="selectAll" resultType="com.campus.parttime.entity.User">
        select * from user
    </select>
    <select id="selectByUsername" resultType="com.campus.parttime.entity.User">
        select * from user where username = #{username}
    </select>
    <insert id="insertUser">
        insert into user(username,password,role,email,phone,status)
        values(#{username},#{password},#{role},#{email},#{phone},#{status})
    </insert>
    <update id="updateUser">
        update user set password=#{password},email=#{email},phone=#{phone},status=#{status} where id=#{id}
    </update>
</mapper>